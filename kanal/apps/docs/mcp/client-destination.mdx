---
title: 'Client destination'
description: 'Stub — full reference lands as the corresponding implementation matures.'
---

The full reference for **Client destination** is published alongside the OpenAPI spec at
`https://docs.kanal.appmcp/client-destination`. This page is a placeholder kept so the
nav graph in `mint.json` validates during early scaffolding.
