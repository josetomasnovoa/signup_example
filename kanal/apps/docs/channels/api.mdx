---
title: 'Api'
description: 'Stub — full reference lands as the corresponding implementation matures.'
---

The full reference for **Api** is published alongside the OpenAPI spec at
`https://docs.kanal.appchannels/api`. This page is a placeholder kept so the
nav graph in `mint.json` validates during early scaffolding.
