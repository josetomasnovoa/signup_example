---
title: 'Whatsapp'
description: 'Stub — full reference lands as the corresponding implementation matures.'
---

The full reference for **Whatsapp** is published alongside the OpenAPI spec at
`https://docs.kanal.appchannels/whatsapp`. This page is a placeholder kept so the
nav graph in `mint.json` validates during early scaffolding.
