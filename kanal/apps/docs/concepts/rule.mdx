---
title: 'Rule'
description: 'Stub — full reference lands as the corresponding implementation matures.'
---

The full reference for **Rule** is published alongside the OpenAPI spec at
`https://docs.kanal.appconcepts/rule`. This page is a placeholder kept so the
nav graph in `mint.json` validates during early scaffolding.
